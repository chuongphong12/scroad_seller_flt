import 'package:flutter/material.dart';

class OngoingDetailScreen extends StatelessWidget {
  const OngoingDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
